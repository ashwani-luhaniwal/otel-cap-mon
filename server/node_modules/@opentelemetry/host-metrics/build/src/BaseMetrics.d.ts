import * as api from '@opentelemetry/api';
import * as metrics from '@opentelemetry/sdk-metrics';
/**
 * Metrics Collector Configuration
 */
export interface MetricsCollectorConfig {
    maxTimeoutUpdateMS?: number;
    meterProvider?: metrics.MeterProvider;
    metricNameSeparator?: string;
    name: string;
    url?: string;
}
export declare const DEFAULT_MAX_TIMEOUT_UPDATE_MS = 500;
/**
 * Base Class for metrics
 */
export declare abstract class BaseMetrics {
    protected _logger: api.DiagAPI;
    protected _maxTimeoutUpdateMS: number;
    protected _meter: api.Meter;
    private _name;
    constructor(config: MetricsCollectorConfig);
    /**
     * Creates metrics
     */
    protected abstract _createMetrics(): void;
    /**
     * Starts collecting stats
     */
    abstract start(): void;
}
//# sourceMappingURL=BaseMetrics.d.ts.map