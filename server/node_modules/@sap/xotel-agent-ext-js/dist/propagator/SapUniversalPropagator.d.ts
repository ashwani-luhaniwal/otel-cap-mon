import { Context, TextMapPropagator, TextMapSetter, TextMapGetter } from '@opentelemetry/api';
export declare class SapUniversalPropagator implements TextMapPropagator {
    inject(context: Context, carrier: unknown, setter: TextMapSetter): void;
    extract(context: Context, carrier: unknown, getter: TextMapGetter): Context;
    fields(): string[];
    private getSpanContextFromHeader;
    private makeCorrelationId;
    private setTraceState;
    private buildPassport;
}
