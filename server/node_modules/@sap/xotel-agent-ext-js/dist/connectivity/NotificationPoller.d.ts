import { Category, NotificationPayload } from "../model/NotificationPayload";
import { configuration } from "../utils/Types";
export declare class NotificationPoller {
    static init(url: string, svc: string, dc: string, time: number): Promise<void>;
    static schedule(): Promise<void>;
    static getPollUrl(): string;
    static pollForNotifications(pollUrl: any): Promise<void>;
    static addToCache(NotificationPayload: NotificationPayload): Promise<void>;
    static isDataCollectionTurnedOn(serviceType: string, tenantId: string, usecaseName: string): boolean;
    static getFilterCategories(serviceType: string, tenantId: string, usecaseName: string): Category[];
    static getCachedMaps(): {
        tenantFilterMap: Map<string, Map<string, configuration>>;
    };
}
