import { InstrumentationBase, InstrumentationModuleDefinition } from '@opentelemetry/instrumentation';
import { HdbInstrumentationConfig } from './types';
export declare class HdbInstrumentation extends InstrumentationBase {
    constructor(config?: HdbInstrumentationConfig);
    protected init(): void | InstrumentationModuleDefinition<any> | InstrumentationModuleDefinition<any>[];
    private _getClientPatches;
    private _getStatementPatches;
    private _patchClientCommit;
    private _patchClientRollback;
    private _patchClientStatement;
    private _patchStatementExec;
    private _patchStatementDrop;
    private _patchCallback;
}
