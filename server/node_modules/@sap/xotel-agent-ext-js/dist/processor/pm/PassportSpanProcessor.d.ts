import { Context } from "@opentelemetry/api";
import { ReadableSpan, Span, SpanExporter, SpanProcessor } from "@opentelemetry/sdk-trace-base";
export declare class PassportSpanProcessor implements SpanProcessor {
    private activeSpans;
    private endedSpans;
    private spanExporter;
    private shutdownOnce;
    constructor(exporter: SpanExporter);
    forceFlush(): Promise<void>;
    onStart(span: Span, parentContext: Context): void;
    onEnd(span: ReadableSpan): void;
    private handleActiveSpans;
    shutdown(): Promise<void>;
    private doShutdown;
    private handlePassport;
    private wasLastActiveSpan;
    private getPassport;
    private doExport;
    private handleFesrSpan;
    private handleNonFesrSpan;
    private setAttributes;
    private normalizeUrl;
}
