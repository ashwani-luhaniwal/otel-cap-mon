import { ExportResult } from "@opentelemetry/core";
import { ReadableSpan, SpanExporter } from "@opentelemetry/sdk-trace-base";
export declare class MSExporter implements SpanExporter {
    private _exporter;
    private buffer;
    private timer;
    private readonly shutdownOnce;
    constructor();
    set exporter(ex: SpanExporter);
    shutdown(): Promise<void>;
    private clearTimer;
    private doShutDown;
    private flush;
    private groupByTenantForMs;
    private exportCallbackForMockService;
    export(spans: ReadableSpan[], resultCallback: (result: ExportResult) => void): void;
    private schedule;
}
