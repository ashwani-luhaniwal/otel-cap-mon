import { ExportResult } from "@opentelemetry/core";
import { ReadableSpan, SpanExporter } from "@opentelemetry/sdk-trace-base";
import { OTLPExporterNodeConfigBase } from '@opentelemetry/otlp-exporter-base';
export declare class CommonOtlpTraceExporter implements SpanExporter {
    private timer;
    private readonly shutdownOnce;
    exporter: SpanExporter;
    buffer: ReadableSpan[];
    private static instance;
    static getInstance(): CommonOtlpTraceExporter;
    private constructor();
    static init(config?: OTLPExporterNodeConfigBase): void;
    static reInit(config?: OTLPExporterNodeConfigBase): void;
    export(spans: ReadableSpan[], resultCallback: (result: ExportResult) => void): void;
    shutdown(): Promise<void>;
    clearTimer(): void;
    private flush;
    private schedule;
    private doShutDown;
}
