"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.IMExporter = void 0;
const core_1 = require("@opentelemetry/core");
const logger_1 = require("../../common/logger");
const NoopSpanExporter_1 = require("../NoopSpanExporter");
const constants = __importStar(require("../../utils/Constants"));
const IMReadableSpan_1 = require("../../model/im/IMReadableSpan");
const resources_1 = require("@opentelemetry/resources");
const TenantKey_1 = require("../../utils/TenantKey");
const INTERVAL = 10 * 1000;
class IMExporter {
    constructor() {
        this.buffer = new Map();
        this._exporter = new NoopSpanExporter_1.NoopSpanExporter();
    }
    set exporter(ex) {
        this._exporter = ex;
    }
    shutdown() {
        return this._exporter.shutdown();
    }
    clearTimer() {
        if (this.timer !== undefined) {
            clearTimeout(this.timer);
            this.timer = undefined;
        }
    }
    flush() {
        this.clearTimer();
        const promises = [];
        const newBuffer = [];
        this.buffer.forEach((value, key) => {
            const singles = value;
            const tenantKey = JSON.parse(key);
            let resource = new resources_1.Resource({
                // [SemanticResourceAttributes.SERVICE_NAME]: constants.CALM_SERVICE_NAME,
                [constants.CALM_ATT_SERVICE_TYPE]: constants.CALM_SERVICE_TYPE,
                [constants.CALM_ATT_USE_CASE]: tenantKey.useCase,
                [constants.CALM_ATT_TENANT_ID]: tenantKey.tenantId,
                [constants.ATT_SUBDOMAIN]: tenantKey.subdomain,
                [constants.CALM_ATT_ACTION_CONTENT]: "",
                [constants.CALM_ATT_ACTION_VERSION]: "",
                [constants.CALM_ATT_ACTION]: "",
            });
            singles.forEach(span => {
                newBuffer.push(new IMReadableSpan_1.IMReadableSpan(span, resource));
            });
        });
        logger_1.logger.debug(`IMExporter: flushing ${newBuffer.length} spans`);
        promises.push(new Promise(this.exportCallbackForImService(newBuffer)));
        return Promise.all(promises)
            .then(() => {
            this.clearTimer();
            this.buffer.clear();
            Promise.resolve();
        })
            .catch(err => Promise.reject(err));
    }
    groupByTenant(imSpans) {
        const map = new Map();
        imSpans.forEach(eachSpan => {
            const tenantId = makeTenantKey(eachSpan);
            const tenantKey = JSON.stringify(tenantId);
            let spans = map.get(tenantKey);
            if (!spans) {
                spans = [];
            }
            spans.push(eachSpan);
            map.set(tenantKey, spans);
        });
        map.forEach((value, key) => {
            let arr = this.buffer.get(key);
            if (!arr) {
                arr = [];
            }
            arr.push(...value);
            this.buffer.set(key, arr);
        });
    }
    exportCallbackForImService(imSpans) {
        return (resolve, reject) => {
            this._exporter.export(imSpans, result => {
                var _a;
                if (result.code === core_1.ExportResultCode.SUCCESS) {
                    resolve();
                }
                else {
                    reject((_a = result.error) !== null && _a !== void 0 ? _a : new Error('IMExporter: span export failed'));
                }
            });
        };
    }
    export(spans, resultCallback) {
        console.log('IMExporter: exporting spans');
        this.groupByTenant(spans);
        this.schedule();
        resultCallback({ code: core_1.ExportResultCode.SUCCESS });
    }
    schedule() {
        if (this.timer !== undefined) {
            return;
        }
        this.timer = setTimeout(() => __awaiter(this, void 0, void 0, function* () {
            try {
                yield this.flush();
            }
            catch (error) {
                console.error(error);
            }
        }), INTERVAL);
        this.timer.unref();
    }
}
exports.IMExporter = IMExporter;
function makeTenantKey(span) {
    // const tenantId = span.resource.attributes[constants.CALM_ATT_TENANT_ID]
    // const subdomain = span.resource.attributes[constants.ATT_SUBDOMAIN]
    // const useCase = span.resource.attributes[constants.CALM_ATT_USE_CASE]
    // Making key for spans of same type of - tenantId, usecase, subdomain
    const tenantId = span.attributes[constants.CALM_ATT_TENANT_ID];
    const subdomain = span.attributes[constants.ATT_SUBDOMAIN];
    const useCase = span.attributes[constants.CALM_ATT_USE_CASE];
    if (tenantId && typeof tenantId === 'string' && useCase && typeof useCase === 'string') {
        if (subdomain && typeof subdomain === "string") {
            return new TenantKey_1.TenantKey(tenantId, useCase, subdomain);
        }
        return new TenantKey_1.TenantKey(tenantId, useCase);
    }
    return new TenantKey_1.TenantKey("", "");
}
