import { SpanKind } from "@opentelemetry/api";
import { SpanType } from "../../common/SpanType";
import { PassportReadableSpan } from "../../model/pm/PassportReadableSpan";
export declare class SpanTuple {
    request: string;
    userId: string;
    action: string;
    spanType?: SpanType;
    spanKind: SpanKind;
    constructor(span: PassportReadableSpan);
}
