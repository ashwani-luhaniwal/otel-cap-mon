import { ExportResult } from "@opentelemetry/core";
import { ReadableSpan, SpanExporter } from "@opentelemetry/sdk-trace-base";
import { SpanAggregator } from "./SpanAggregator";
export declare class AggregatingOtlpTraceExporter implements SpanExporter {
    private _exporter;
    private readonly account;
    private readonly buffer;
    private timer;
    private readonly shutdownOnce;
    private aggregator;
    constructor(aggregator: SpanAggregator, account?: string);
    set exporter(ex: SpanExporter);
    export(spans: ReadableSpan[], resultCallback: (result: ExportResult) => void): void;
    shutdown(): Promise<void>;
    private flush;
    private doShutDown;
    private schedule;
    private clearTimer;
    private groupByTenant;
    private exportCallback;
}
