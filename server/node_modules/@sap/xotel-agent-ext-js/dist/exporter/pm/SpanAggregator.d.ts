import { PassportReadableSpan } from "../../model/pm/PassportReadableSpan";
import { AggregatedSpan } from "../../model/pm/AggregatedSpan";
export interface AggregationDto {
    aggregated: AggregatedSpan[];
    singles: PassportReadableSpan[];
}
export declare class SpanAggregator {
    aggregate(singles: PassportReadableSpan[], tenantId: string, account: string, useCase: string, subdomain?: string): AggregationDto;
    private shouldAggregate;
    private addExternalInfo;
    private enrichIncoming;
    private groupByTraceId;
    private groupByTuple;
    private doAggregate;
    private getActionKey;
    private getRequestKey;
}
