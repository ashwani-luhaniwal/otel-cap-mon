import { ExportResult } from "@opentelemetry/core";
import { AggregationTemporality, InstrumentType, PushMetricExporter, ResourceMetrics } from "@opentelemetry/sdk-metrics";
export declare class HealthMetricExporter implements PushMetricExporter {
    private _exporter;
    constructor();
    set exporter(ex: PushMetricExporter);
    export(metrics: ResourceMetrics, resultCallback: (result: ExportResult) => void): void;
    forceFlush(): Promise<void>;
    selectAggregationTemporality?(instrumentType: InstrumentType): AggregationTemporality;
    shutdown(): Promise<void>;
}
