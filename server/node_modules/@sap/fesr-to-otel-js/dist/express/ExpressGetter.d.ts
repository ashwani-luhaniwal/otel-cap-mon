import { TextMapGetter } from "@opentelemetry/api";
import { Request } from "express";
export declare class ExpressGetter implements TextMapGetter<Request<{}, any, any, any, Record<string, any>>> {
    keys(carrier: Request): string[];
    get(carrier: Request, key: string): string | string[] | undefined;
}
